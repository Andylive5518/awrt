--- a/Makefile
+++ b/Makefile
@@ -11,8 +11,8 @@ LUCI_DEPENDS:=@(x86_64||aarch64) +curl +opkg +luci-lib-ipkg +tar +libuci-lua +mo
 LUCI_EXTRA_DEPENDS:=luci-lib-taskd (>=1.0.19)
 LUCI_PKGARCH:=all

-PKG_VERSION:=0.1.32-1
+PKG_VERSION:=0.1.32
 # PKG_RELEASE MUST be empty for luci.mk
-PKG_RELEASE:=
+PKG_RELEASE:=1

 ISTORE_UI_VERSION:=0.1.32
 ISTORE_UI_RELEASE:=1
